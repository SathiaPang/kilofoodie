import 'package:flutter/material.dart';

class PlaceOrderBox extends StatelessWidget {
  const PlaceOrderBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}